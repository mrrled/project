class Column
{
    field char name;
    field Array cells;

    constructor Column new(char symbol, int countRows)
    {
        let name = symbol;
        let cells = Array.new(countRows);
        return this;
    }
}